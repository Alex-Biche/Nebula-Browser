#include <iostream>
#include "parser.h"

// Placeholder parser (just outputs the first 200 characters)
std::string parseHTML(const std::string& html) {
    return html.substr(0, 200) + "... [truncated]";
}
